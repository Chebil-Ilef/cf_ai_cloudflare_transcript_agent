export async function ingestTranscript(
  _env: Record<string, unknown>,
  _params: Record<string, unknown>
) {
  // placeholder: validate, dedupe, and queue for processing
  return { ok: true, queued: true };
}
